<?xml version="1.0" encoding="utf-8" ?>
<prism:PrismApplicationBase xmlns="http://xamarin.com/schemas/2014/forms"
                             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                             xmlns:prism="clr-namespace:Prism;assembly=Prism.Forms"
                             xmlns:toolbarTest="clr-namespace:ToolbarTest;assembly=ToolbarTest"
                             x:Class="ToolbarTest.App">
    <Application.Resources>

        <toolbarTest:ToolbarView x:Key="ToolbarViewKey" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" BackgroundColor="MediumVioletRed" />

        <ControlTemplate x:Key="MainPagePresenter">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ContentView Grid.Row="0" Content="{StaticResource ToolbarViewKey}" BindingContext="{TemplateBinding BindingContext}" />
                <ContentPresenter Grid.Row="1" />
            </Grid>
        </ControlTemplate>

    </Application.Resources>
</prism:PrismApplicationBase>